[pytest]
# The Run/tests files are native assertion runners (their module docstrings
# explicitly say they are not pytest tests).  Keep them runnable directly via
# `python Run/tests/<file>.py`, but do not collect them as pytest modules.
testpaths = API/tests Combat/tests
addopts = --import-mode=importlib
norecursedirs = Outputs .git .codex .claude
